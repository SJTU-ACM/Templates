for(i=2;i<=n;i++){//从2到n枚举i
        if(!f[i])p[++t]=i;//i没有被筛到过，是质数
        for(j=1;j<=t&&i*p[j]<=n;j++){//筛选
            f[i*p[j]]=1;//i*p[j]是合数，标记
            if(i%p[j]==0)break;//优化，i%p[j]=0时，不用接着往下枚举
        }
 }
